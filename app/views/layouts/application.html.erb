<!DOCTYPE html>
<html>
  <head>
    <title>RailsSimpleAirbnb2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.1.2/css/all.css">

  </head>

  <body>
    <%# Nav bar %>
    <div class="d-flex p-3 bg-light justify-content-between align-items-center">
      <%= link_to 'Simple Airbnb', flats_path, class: 'text-decoration-none text-dark fw-bold'  %>
      <div class="search-add d-flex">
        <%= yield :search_section %>
        <%= link_to 'Add a flat', new_flat_path, class: 'btn btn-outline-primary mx-1' %>
      </div>
    </div>

    <%# image for show page %>
    <%= yield :image_section %>

    <%# normal layout container %>
    <div class="container my-3">
      <%= yield %>
    </div>
  </body>
</html>
